const EmptyStateMessage: React.FC = () => {
  return (
    <div className="empty-state">
      <h2>No events to display</h2>
      <p>Start adding events to see them here.</p>
    </div>
  );
};

export default EmptyStateMessage;
